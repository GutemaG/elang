import 'package:flutter/widgets.dart';

/// Non-Web stub. Never actually rendered — call sites gate on `kIsWeb`
/// before building this widget at all — but the conditional-import target
/// must still exist so non-Web platforms have something to compile against
/// instead of the real `dart:ui_web`/`package:web`-based implementation.
Widget renderGoogleWebButton() => const SizedBox.shrink();
